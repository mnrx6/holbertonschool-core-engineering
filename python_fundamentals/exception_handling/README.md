# Python - Exception Handling

This project focuses on handling runtime errors in Python using exceptions.

In this project, I learned how to:

* Use `try` and `except`
* Handle specific exceptions
* Use `else` and `finally`
* Raise exceptions with `raise`
* Write safer code that can handle unexpected errors

All files are written in Python 3 and follow the project requirements.

