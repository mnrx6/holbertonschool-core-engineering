# Python - Control Flow

This project is about learning control flow in Python.

In this project, I learned how to use:

* `if`, `elif`, and `else`
* `for` and `while` loops
* Conditional statements
* Basic Python syntax

These concepts help control how a Python program runs and makes decisions.

